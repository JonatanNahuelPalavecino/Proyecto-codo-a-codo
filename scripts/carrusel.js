const splide = new Splide( '.splide', {
    type   : 'loop',
    perPage: 1,
    focus  : 'center',
    autoplay: true,
});
  
splide.mount();



const splide2 = new Splide( '.another-splide', {
    type   : 'loop',
    perPage: 1,
    focus  : 'center',
    autoplay: true,
});
  
splide2.mount();

